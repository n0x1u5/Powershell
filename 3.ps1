$numero=Read-Host "Introduce un numero" #Lo que se inserte por teclado lo guarda en la variable $numero
Write-Host "El numero es: $numero" #Muestra el numero introducido

#foreach ($a in 1..$max) {
#    write-host $a
# }
